p.lead
  = repo.owner_and_name

== table_bones do
  tr
    th = 'File Name'
    th = 'Resource'
    th = 'Lines For Person'

  - repo.biggest_silos.each do |silo|
    tr
      td = silo.file_name
      td = silo.person
      td = silo.lines_for_person
